{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core invoke/event (frontend), dialogs and shell are used Rust-side",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
